Update the existing `G:\مركز تدريب\AGENTS.md` (it already exists; no `.zcode/AGENTS.md` or `.agents/AGENTS.md` present). Preserve all current content (Role, Language Policy, Core Directives 1-5, Deployment/Testing notes) verbatim, then append concise project-specific technical sections so future agents don't have to re-derive them:

1. **Repository Layout** — npm workspace with three pillars (`backend` Express+libSQL/Turso API, `admin-ui` Next.js 14 dashboard, `user-ui` Next.js 14 student app), root `/tests` Playwright dir, root `tsconfig.base.json` (not extended by pillars).
2. **Commands** — root helper scripts (`dev:backend/admin/user`, `build:*`, `lint`, `format`); per-pillar scripts (`dev`/`build`/`start`/`lint`). Note absence of `typecheck`/`test` scripts and the exact `npx tsc --noEmit` workaround.
3. **Architecture Boundaries & Rules** — separate token namespaces (`token` vs `admin_token`); both UIs call the same API via each pillar's `lib/api.ts` (15s AbortController timeout, auth from localStorage); `(dashboard)` route group is the real guarded surface; admin protection is client-side via `ClientLayout`; QR/attendance workflow lives in the admin course-detail page.
4. **Coding Conventions** — Tailwind + CSS-variable theming (brand colors injected at runtime from `/api/branding`), RTL hardcoded `<html lang="ar" dir="rtl">`, native `fetch` (no axios), images via base64/`<img>` (no `next/image`), route pattern in backend.
5. **Environment Variables** — backend (`TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN`, `JWT_SECRET`, `FRONTEND_URL`, `ADMIN_EMAIL`/`ADMIN_PASSWORD`, optional `GEMINI_API_KEY`); both UIs (`NEXT_PUBLIC_API_URL`); note `file:./data.db` local fallback.
6. **Live Deployment** — Vercel domains for admin and user UIs; backend on `@vercel/node` via `api/index.ts`; `--prod` only, no preview URLs.
7. **Gotchas** — schema/migrations run inline at boot in `connection.ts` (not versioned); CORS allowlist source is `backend/src/index.ts`; `admin-ui` `groups/` route is a redirect stub and `reports/` dir is empty; `backend/data.db` is committed; empty top-level `app/account|cart|dashboard|orders` dirs in user-ui are vestigial.

Approach: use Edit against the existing file (Read already done in this session), appending the new sections after the current Deployment/Testing note. Target length ~90-110 lines total. Keep all of the user's existing behavioral directives unchanged.